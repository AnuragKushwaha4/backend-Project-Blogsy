<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">
</head>

<body>
    <div class="bg-zinc-900 text-white w-full min-h-screen">
        <div class="w-full flex justify-between bg-gradient-to-b from-zinc-800 via-zinc-900 to-zinc-950 p-3 mb-4  ">
            <h1 class="text-3xl text-blue-500 font-[Pacifico]">Blogsy.com</h1>
            <a href="/profile" class="text-blue-500 font-[Pacifico] text-xl">Profile</a>
            <a href="/logout" class="bg-red-600 mt-2  px-3 py-2 ">Logout</a>
        </div>
        <div>
            <h1 class="font-bold font-Merriweather text-3xl mb-2">Hello</h1>
            <h1 class="ml-5 font-bold text-2xl">
                <%= user.name %> ✌️
            </h1>
        </div>
        <div class="CreatePost mt-10">
            <h1 class="mb-2 font-bold font-Merriweather text-2xl">Create Post</h1>
            <form action="/post1" method="post">
                <textarea name="content"
                    class="resize-none w-2/3 h-20 border-2 border-zinc-800 bg-transparent rounded-md block"
                    placeholder="Write your post..."></textarea>
                <input type="submit" value="Create Post" class="mt-2 bg-blue-500 px-3 py-2 block">
            </form>
        </div>

        <div class=" w-full mt-16">
            <h1 class="mb-4 font-bold font-Merriweather text-2xl mt-3">Posts</h1>
            <% if(posts.length>0){%>
                <% posts.reverse().forEach((val)=>{%>
                    <div class="border-2 px-3 py-2 w-2/3 mt-3 border-zinc-800 bg-zinc-700">
                        <div class="flex justify-between">
                            <a href="/viewing/<%= val.user.email%>">
                                <h2 class="text-blue-500 text-xl">@<%=val.user.username %>
                                </h2>
                            </a>
                            <% if (String(user._id) !==String(val.user._id)) { %>
                                <a href="/follow1/<%= val.user._id %>" class="<%= (val.user.followers.indexOf(user._id)===-1) ? 'bg-blue-500 text-white px-6 py-2 rounded-md' : 'bg-transparent border border-blue-500 text-blue-500 px-6 py-2  rounded-md' %>"><%= (val.user.followers.indexOf(user._id)===-1) ? "Follow" : "Following" %></a>
                            <% } %>
                        </div>
                        <p>
                            <%= val.content%>
                        </p>
                        <div class="flex justify-between mt-5 text-xl">
                            <a href="/like1/<%= val._id %>" class=" text-blue-500">
                                <%= val.likes.length%>
                                    <%= val.likes.indexOf(user._id)===-1?"Like":"Liked"%>
                            </a>
                            <a class="text-zinc-900  font-bold" href="/edit/<%= val._id%>">
                                <%= String(val.user._id)==String(user._id)?"Edit":""%>
                            </a>
                        </div>
                    </div>
                    <%})%>
                        <% } else{ %>
                            <h1>No post yet</h1>
                            <% } %>

        </div>
    </div>
</body>

</html>